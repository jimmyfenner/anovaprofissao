# anovaprofissao.com.br

Site estático. Sem framework, sem dependências.

```bash
node build.js      # gera dist/
npx serve dist     # olhar local
```

Deploy automático na Vercel a cada push na `main`.
Contexto completo do projeto: veja `CLAUDE.md`.
